# Frontend env — Vite reads VITE_* vars at build time

# Base URL for API calls. Leave as /api to use Vite's dev proxy (recommended for dev).
# In production, set this to the gateway's full URL.
VITE_API_BASE_URL=/api
